name: Deploy SAP AI Assistant

on:
  push:
    branches:
      - main
<<<<<<< HEAD

jobs:
  deploy:
    name: Auto Deploy to Linux Server
    runs-on: self-hosted
=======
  pull_request:
  # Jalur manual: dipakai bila runner GitHub tidak tersedia (kuota menit habis
  # pada repo privat) sehingga job verify tidak pernah selesai dan deploy ikut
  # tertahan. Jalankan dari tab Actions -> Verify & Deploy -> Run workflow.
  workflow_dispatch:
    inputs:
      skip_verify:
        description: "Lewati pemeriksaan dan langsung deploy (darurat saja)"
        type: boolean
        default: false

jobs:
  # Gerbang verifikasi. Sebelumnya setiap push ke main langsung dideploy tanpa
  # pemeriksaan apa pun, sehingga commit yang merusak frontend (mis. variabel
  # yang tidak dideklarasikan) bisa mendarat di server sebagai halaman putih.
  verify:
    name: Build, Lint & Test
    if: github.event_name != 'workflow_dispatch' || !inputs.skip_verify
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: ABAP_DB
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Lint frontend
        working-directory: frontend
        run: npx oxlint src/ --deny-warnings

      - name: Build frontend
        working-directory: frontend
        run: npm run build

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install backend dependencies
        run: pip install -r backend/requirements.txt

      - name: Run backend tests
        env:
          TEST_DATABASE_URL: postgresql+psycopg://postgres:postgres@127.0.0.1:5432/ABAP_DB
          JWT_SECRET: ci-secret-ci-secret-ci-secret-1234
          BOOTSTRAP_ADMIN_PASSWORD: AdminPass123
        run: pytest

  deploy:
    name: Auto Deploy
    needs: verify
    # Deploy berjalan bila verifikasi lulus, atau saat dijalankan manual dengan
    # skip_verify (job verify di-skip, bukan gagal — karena itu perlu always()).
    if: >-
      always()
      && github.ref == 'refs/heads/main'
      && (needs.verify.result == 'success'
          || (github.event_name == 'workflow_dispatch' && inputs.skip_verify))
    # Menggunakan label spesifik agar terisolasi dari runner project lain di server yang sama
    runs-on: [self-hosted, sap-ai]
>>>>>>> 7e7cfff (fix: scrollable tables on mobile, local dev setup, and a manual deploy path)
    steps:
      - name: Execute Update Script on Server
        run: |
          echo "🚀 [GitHub Runner] Memulai Auto Deployment SAP AI Assistant..."
          echo "   Hasil verifikasi: ${{ needs.verify.result }}"
          cd /var/www/SAP-AI-Assistant
          chmod +x deploy/update.sh
          bash deploy/update.sh
